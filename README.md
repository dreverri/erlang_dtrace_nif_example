Erlang DTrace NIF Example
=========================

This is an example Erlang module that uses a NIF to interact with DTrace.

Usage
-----

Start example.d:

      $ ./example.d

In a seperate terminal run the eunit tests:

      $ ./rebar compile eunit
